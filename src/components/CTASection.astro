---
---

<div class="section-divider"></div>

<section id="cta" class="py-24 md:py-28 px-6 text-center relative overflow-hidden" style="background: var(--bg-deep);">
  <div class="absolute bottom-0 left-1/2 -translate-x-1/2 w-[700px] h-[400px] pointer-events-none" style="background: radial-gradient(ellipse, rgba(167,139,250,0.04), transparent 70%);"></div>

  <div class="max-w-6xl mx-auto relative z-10">
    <div class="reveal">
      <div class="section-label justify-center">Join Us</div>
      <h2 class="section-title" style="max-width: 460px; margin: 0 auto 12px;">想拥有自己的 AI 团队？</h2>
      <p class="section-desc mx-auto text-center mb-12">关注公众号获取完整教程，加入社群与 AI 玩家交流。</p>
    </div>

    <div class="flex gap-5 justify-center flex-wrap reveal reveal-delay-1">
      <!-- 公众号 -->
      <div class="cta-card group rounded-xl text-center cursor-pointer transition-all duration-200" style="width: 260px; padding: 32px 28px; background: var(--bg-card); border: 1px solid var(--border-subtle);">
        <div class="w-12 h-12 rounded-xl flex items-center justify-center mx-auto mb-4 transition-all duration-200" style="background: rgba(34,211,238,0.06); border: 1px solid rgba(34,211,238,0.12);">
          <svg class="w-6 h-6" viewBox="0 0 24 24" fill="none" stroke="var(--accent-cyan)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <rect x="5" y="2" width="14" height="20" rx="2" ry="2"></rect>
            <line x1="12" y1="18" x2="12.01" y2="18"></line>
          </svg>
        </div>
        <h4 class="font-semibold mb-2 text-[0.95rem]" style="color: var(--text-primary); font-family: var(--font-heading);">公众号</h4>
        <p class="text-[0.82rem] leading-relaxed mb-4" style="color: var(--text-secondary);">「知屿AI编程」<br />教程、思考、实战分享</p>
        <div class="mx-auto overflow-hidden flex items-center justify-center" style="width: 120px; height: 120px; background: rgba(255,255,255,0.02); border: 1px dashed var(--border-light); border-radius: 12px;">
          <span class="text-[0.7rem]" style="color: var(--text-muted); font-family: var(--font-mono);">二维码</span>
        </div>
      </div>

      <!-- 微信群 -->
      <div class="cta-card group rounded-xl text-center cursor-pointer transition-all duration-200" style="width: 260px; padding: 32px 28px; background: var(--bg-card); border: 1px solid var(--border-subtle);">
        <div class="w-12 h-12 rounded-xl flex items-center justify-center mx-auto mb-4 transition-all duration-200" style="background: rgba(167,139,250,0.06); border: 1px solid rgba(167,139,250,0.12);">
          <svg class="w-6 h-6" viewBox="0 0 24 24" fill="none" stroke="var(--accent-purple)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"></path>
          </svg>
        </div>
        <h4 class="font-semibold mb-2 text-[0.95rem]" style="color: var(--text-primary); font-family: var(--font-heading);">加微信进群</h4>
        <p class="text-[0.82rem] leading-relaxed mb-4" style="color: var(--text-secondary);">AI Agent 实践者社区<br />一起探索 AI 的边界</p>
        <div class="mx-auto overflow-hidden flex items-center justify-center" style="width: 120px; height: 120px; background: rgba(255,255,255,0.02); border: 1px dashed var(--border-light); border-radius: 12px;">
          <span class="text-[0.7rem]" style="color: var(--text-muted); font-family: var(--font-mono);">二维码</span>
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  document.querySelectorAll<HTMLElement>('.cta-card').forEach(card => {
    card.addEventListener('mouseenter', () => {
      card.style.borderColor = 'rgba(167,139,250,0.2)';
      card.style.transform = 'translateY(-4px)';
      card.style.boxShadow = '0 12px 40px rgba(167,139,250,0.15)';
    });
    card.addEventListener('mouseleave', () => {
      card.style.borderColor = '';
      card.style.transform = '';
      card.style.boxShadow = '';
    });
  });
</script>
