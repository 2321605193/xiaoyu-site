---
---

<div class="section-divider"></div>

<section id="cta" class="py-24 md:py-28 px-6 text-center relative overflow-hidden" style="background: var(--bg-deep);">
  <!-- Background glow -->
  <div class="absolute bottom-0 left-1/2 -translate-x-1/2 w-[700px] h-[400px] pointer-events-none" style="background: radial-gradient(ellipse, rgba(167,139,250,0.05), transparent 70%);"></div>
  <div class="absolute top-0 left-1/2 -translate-x-1/2 w-[500px] h-[300px] pointer-events-none" style="background: radial-gradient(ellipse, rgba(1,205,254,0.04), transparent 70%);"></div>

  <div class="max-w-6xl mx-auto relative z-10">
    <div class="reveal">
      <div class="section-label justify-center">Join Us</div>
      <h2 class="section-title neon-glow-pink" style="max-width: 460px; margin: 0 auto 12px;">想拥有自己的 AI 团队？</h2>
      <p class="section-desc mx-auto text-center mb-12">关注公众号获取完整教程，加入社群与 AI 玩家交流。</p>
    </div>

    <div class="flex gap-5 justify-center flex-wrap reveal reveal-delay-1">
      <!-- 公众号 -->
      <div class="cta-card glass-card group rounded-2xl text-center cursor-pointer transition-all duration-300" style="width: 280px; padding: 36px 28px;">
        <div class="w-12 h-12 rounded-xl flex items-center justify-center mx-auto mb-4 transition-all duration-300 group-hover:scale-110" style="background: rgba(1,205,254,0.1); border: 1px solid rgba(1,205,254,0.2); box-shadow: 0 0 15px rgba(1,205,254,0.2);">
          <svg class="w-6 h-6" viewBox="0 0 24 24" fill="none" stroke="var(--accent-cyan)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <rect x="5" y="2" width="14" height="20" rx="2" ry="2"></rect>
            <line x1="12" y1="18" x2="12.01" y2="18"></line>
          </svg>
        </div>
        <h4 class="font-semibold mb-1.5 text-[0.95rem]" style="color: var(--text-primary); font-family: var(--font-heading);">公众号</h4>
        <p class="text-[0.82rem] leading-relaxed mb-5" style="color: var(--text-secondary);">「知屿AI编程」<br />教程、思考、实战分享</p>

        <!-- QR Code with scan animation -->
        <div class="qr-container mx-auto relative overflow-hidden" style="width: 128px; height: 128px; background: rgba(255,255,255,0.02); border: 1px solid rgba(1,205,254,0.15); border-radius: 12px;">
          <div class="qr-corners">
            <span class="qr-corner qr-corner--tl"></span>
            <span class="qr-corner qr-corner--tr"></span>
            <span class="qr-corner qr-corner--bl"></span>
            <span class="qr-corner qr-corner--br"></span>
          </div>
          <div class="qr-scanline" style="background: linear-gradient(to bottom, transparent, rgba(1,205,254,0.4), transparent);"></div>
          <div class="flex items-center justify-center h-full">
            <span class="text-[0.65rem]" style="color: var(--text-muted); font-family: var(--font-mono); letter-spacing: 0.05em;">扫码关注</span>
          </div>
        </div>
      </div>

      <!-- 微信群 -->
      <div class="cta-card glass-card group rounded-2xl text-center cursor-pointer transition-all duration-300" style="width: 280px; padding: 36px 28px;">
        <div class="w-12 h-12 rounded-xl flex items-center justify-center mx-auto mb-4 transition-all duration-300 group-hover:scale-110" style="background: rgba(185,103,255,0.1); border: 1px solid rgba(185,103,255,0.2); box-shadow: 0 0 15px rgba(185,103,255,0.2);">
          <svg class="w-6 h-6" viewBox="0 0 24 24" fill="none" stroke="var(--accent-purple)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"></path>
          </svg>
        </div>
        <h4 class="font-semibold mb-1.5 text-[0.95rem]" style="color: var(--text-primary); font-family: var(--font-heading);">加微信进群</h4>
        <p class="text-[0.82rem] leading-relaxed mb-5" style="color: var(--text-secondary);">AI Agent 实践者社区<br />一起探索 AI 的边界</p>

        <!-- QR Code with scan animation -->
        <div class="qr-container mx-auto relative overflow-hidden" style="width: 128px; height: 128px; background: rgba(255,255,255,0.02); border: 1px solid rgba(185,103,255,0.15); border-radius: 12px;">
          <div class="qr-corners" style="--qr-color: rgba(185,103,255,0.8);">
            <span class="qr-corner qr-corner--tl"></span>
            <span class="qr-corner qr-corner--tr"></span>
            <span class="qr-corner qr-corner--bl"></span>
            <span class="qr-corner qr-corner--br"></span>
          </div>
          <div class="qr-scanline" style="background: linear-gradient(to bottom, transparent, rgba(185,103,255,0.4), transparent); animation-delay: -1.5s;"></div>
          <div class="flex items-center justify-center h-full">
            <span class="text-[0.65rem]" style="color: var(--text-muted); font-family: var(--font-mono); letter-spacing: 0.05em;">扫码进群</span>
          </div>
        </div>
      </div>
    </div>

    <!-- Bottom note -->
    <p class="mt-10 text-[0.72rem] reveal reveal-delay-2" style="color: var(--text-muted); font-family: var(--font-mono);">
      加入 <span class="neon-glow-cyan" style="color: var(--accent-cyan);">100+</span> AI 实践者 · 共同探索 AI 边界
    </p>
  </div>
</section>

<style>
  /* QR Corner markers */
  .qr-corners {
    position: absolute;
    inset: 0;
    --qr-color: rgba(1, 205, 254, 0.8);
  }
  .qr-corner {
    position: absolute;
    width: 14px;
    height: 14px;
  }
  .qr-corner--tl { top: 8px; left: 8px; border-top: 2px solid var(--qr-color); border-left: 2px solid var(--qr-color); }
  .qr-corner--tr { top: 8px; right: 8px; border-top: 2px solid var(--qr-color); border-right: 2px solid var(--qr-color); }
  .qr-corner--bl { bottom: 8px; left: 8px; border-bottom: 2px solid var(--qr-color); border-left: 2px solid var(--qr-color); }
  .qr-corner--br { bottom: 8px; right: 8px; border-bottom: 2px solid var(--qr-color); border-right: 2px solid var(--qr-color); }

  /* Scan line animation */
  .qr-scanline {
    position: absolute;
    left: 0;
    right: 0;
    height: 20px;
    animation: qr-scan 2.5s linear infinite;
  }
  @keyframes qr-scan {
    0% { top: -10px; opacity: 0; }
    10% { opacity: 1; }
    90% { opacity: 1; }
    100% { top: calc(100% + 10px); opacity: 0; }
  }
  @media (prefers-reduced-motion: reduce) {
    .qr-scanline { animation: none; top: 50%; opacity: 0.3; }
  }

  .cta-card:hover {
    border-color: rgba(1, 205, 254, 0.4);
    transform: translateY(-6px);
    box-shadow: 0 0 30px rgba(1, 205, 254, 0.25), 0 12px 40px rgba(0, 0, 0, 0.6);
  }
</style>
