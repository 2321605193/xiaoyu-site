---
const navItems = [
  { label: '团队', href: '#team' },
  { label: '能力', href: '#capabilities' },
  { label: '动态', href: '#worklog' },
  { label: '博客', href: '/blog' },
  { label: '关于', href: '#about' },
];
---

<nav class="fixed top-0 left-0 right-0 z-50 transition-all duration-300" id="navbar" style="padding: 16px 0;">
  <div class="max-w-[1080px] mx-auto px-6 flex items-center justify-between">
    <a href="/" class="flex items-center gap-2 font-bold cursor-pointer" style="font-size: 1.1rem; color: var(--text-primary);">
      <span class="inline-block w-2 h-2 rounded-full" style="background: var(--accent-cyan); box-shadow: 0 0 8px var(--accent-cyan), 0 0 20px rgba(34, 211, 238, 0.3); animation: pulse-glow 3s ease-in-out infinite;"></span>
      🏝️ 小屿
    </a>

    <div class="hidden md:flex gap-7">
      {navItems.map(item => (
        <a href={item.href} class="transition-colors duration-200 cursor-pointer"
          style="color: var(--text-secondary); font-size: 0.875rem; font-weight: 500; letter-spacing: 0.01em;"
          onmouseover="this.style.color='var(--text-primary)'" onmouseout="this.style.color='var(--text-secondary)'">{item.label}</a>
      ))}
    </div>

    <button class="md:hidden cursor-pointer" id="menu-btn" aria-label="菜单" style="color: var(--text-secondary);">
      <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M4 6h16M4 12h16M4 18h16" />
      </svg>
    </button>
  </div>

  <div class="hidden md:hidden" id="mobile-menu">
    <div class="px-6 py-3 space-y-1" style="background: rgba(8,11,20,0.95); backdrop-filter: blur(20px); border-top: 1px solid var(--border-subtle);">
      {navItems.map(item => (
        <a href={item.href} class="block py-2 px-3 rounded-lg cursor-pointer"
          style="color: var(--text-secondary); font-size: 0.875rem; font-weight: 500;">{item.label}</a>
      ))}
    </div>
  </div>
</nav>

<script>
  const navbar = document.getElementById('navbar');
  const menuBtn = document.getElementById('menu-btn');
  const mobileMenu = document.getElementById('mobile-menu');

  window.addEventListener('scroll', () => {
    if (window.scrollY > 50) {
      navbar.style.padding = '10px 0';
      navbar.style.background = 'rgba(8, 11, 20, 0.85)';
      navbar.style.backdropFilter = 'blur(20px)';
      navbar.style.borderBottom = '1px solid var(--border-subtle)';
    } else {
      navbar.style.padding = '16px 0';
      navbar.style.background = 'transparent';
      navbar.style.backdropFilter = 'none';
      navbar.style.borderBottom = 'none';
    }
  });

  menuBtn?.addEventListener('click', () => mobileMenu?.classList.toggle('hidden'));
  mobileMenu?.querySelectorAll('a').forEach(l => l.addEventListener('click', () => mobileMenu?.classList.add('hidden')));
</script>
