---
const navItems = [
  { label: '首页', href: '#hero' },
  { label: '团队', href: '#team' },
  { label: '能力', href: '#capabilities' },
  { label: '动态', href: '#worklog' },
  { label: '博客', href: '/blog' },
  { label: '关于', href: '#about' },
];
---

<nav class="fixed top-0 w-full z-50 transition-all duration-500" id="navbar">
  <div class="max-w-6xl mx-auto px-6 lg:px-8">
    <div class="flex items-center justify-between h-16">
      <a href="/" class="flex items-center gap-2 text-lg font-heading font-bold group">
        <span class="group-hover:scale-110 transition-transform duration-300">🏝️</span>
        <span class="text-sea-text">小屿</span>
      </a>

      <!-- Desktop nav -->
      <div class="hidden md:flex items-center gap-1">
        {navItems.map(item => (
          <a href={item.href}
            class="text-sea-muted hover:text-sea-cyan px-3.5 py-2 rounded-lg hover:bg-white/[0.04] transition-all duration-200 text-sm font-medium">
            {item.label}
          </a>
        ))}
      </div>

      <!-- Mobile menu button -->
      <button class="md:hidden text-sea-muted hover:text-sea-cyan p-2 rounded-lg hover:bg-white/[0.04] transition-all" id="menu-btn" aria-label="菜单">
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M4 6h16M4 12h16M4 18h16" />
        </svg>
      </button>
    </div>
  </div>

  <!-- Mobile menu -->
  <div class="hidden md:hidden border-t border-white/[0.04]" id="mobile-menu">
    <div class="px-6 py-3 space-y-1 bg-sea-dark/95 backdrop-blur-xl">
      {navItems.map(item => (
        <a href={item.href}
          class="block text-sea-muted hover:text-sea-cyan hover:bg-white/[0.04] transition-all py-2.5 px-3 rounded-lg text-sm font-medium">
          {item.label}
        </a>
      ))}
    </div>
  </div>
</nav>

<script>
  const navbar = document.getElementById('navbar');
  const menuBtn = document.getElementById('menu-btn');
  const mobileMenu = document.getElementById('mobile-menu');

  window.addEventListener('scroll', () => {
    if (window.scrollY > 50) {
      navbar?.classList.add('bg-sea-dark/80', 'backdrop-blur-xl', 'border-b', 'border-white/[0.04]');
    } else {
      navbar?.classList.remove('bg-sea-dark/80', 'backdrop-blur-xl', 'border-b', 'border-white/[0.04]');
    }
  });

  menuBtn?.addEventListener('click', () => {
    mobileMenu?.classList.toggle('hidden');
  });

  mobileMenu?.querySelectorAll('a').forEach(link => {
    link.addEventListener('click', () => mobileMenu?.classList.add('hidden'));
  });
</script>
