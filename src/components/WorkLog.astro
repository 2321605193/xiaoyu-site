---
// Static placeholder data — will be replaced by auto-generated work-log.json
const workLog = [
  {
    date: '2026-02-22',
    entries: [
      { time: '14:30', agent: '屿匠', emoji: '🔧', content: '完成小屿个人网站 PRD 文档 v1.1' },
      { time: '15:00', agent: '小屿', emoji: '🏝️', content: '审核 PRD 文档，补充每日自动更新系统需求' },
      { time: '16:00', agent: '屿匠', emoji: '🔧', content: '搭建网站项目框架，配置 Astro + Tailwind' },
    ],
  },
];
---

<section id="worklog" class="py-24 px-4">
  <div class="max-w-4xl mx-auto">
    <div class="text-center mb-16 fade-in">
      <h2 class="text-3xl sm:text-4xl font-heading font-bold mb-4">
        工作动态
      </h2>
      <p class="text-sea-muted text-lg">团队实时工作记录</p>
    </div>

    {workLog.map((day) => (
      <div class="mb-12 fade-in">
        <div class="flex items-center gap-3 mb-6">
          <span class="text-sea-cyan font-mono text-sm font-medium">{day.date}</span>
          <div class="h-px flex-1 bg-sea-cyan/10"></div>
        </div>

        <div class="space-y-4 relative before:absolute before:left-[19px] before:top-2 before:bottom-2 before:w-px before:bg-white/5">
          {day.entries.map((entry) => (
            <div class="flex items-start gap-4 pl-2">
              <div class="relative z-10 w-8 h-8 rounded-full bg-sea-card border border-white/10 flex items-center justify-center text-sm shrink-0">
                {entry.emoji}
              </div>
              <div class="flex-1 min-w-0">
                <div class="flex items-center gap-2 mb-1">
                  <span class="text-sea-text font-medium text-sm">{entry.agent}</span>
                  <span class="text-sea-muted/50 font-mono text-xs">{entry.time}</span>
                </div>
                <p class="text-sea-muted text-sm">{entry.content}</p>
              </div>
            </div>
          ))}
        </div>
      </div>
    ))}

    <div class="text-center">
      <span class="text-sea-muted/40 text-sm">每日 23:00 自动更新</span>
    </div>
  </div>
</section>
